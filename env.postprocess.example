# === Postprocess (Stage 1) ===
POST_MIN_CHARS=48
POST_MERGE_GAP_MS=900
POST_MAX_PAR_LEN=1200
POST_STRICT_DEDUP=1
POST_FUZZY_DEDUP=1
POST_FILLERS_RU="ну,как бы,то есть,ээ,эээ,короче,значит,в общем"
POST_FILLERS_EN="uh,um,like,you know,so,kinda,sort of"
POST_WRITE_CLEAN_TXT=0
POST_REPORT=1
# TRANSCRIBE_OUT defaults to ./transcribe if not set
