# === Edit Transcript (Stage 2.5) ===

# LLM для редактирования
EDIT_LLM_MODEL=qwen3:32b
EDIT_LLM_TEMPERATURE=0.3
EDIT_LLM_MAX_TOKENS=3000             # максимум токенов на главу

# Контроль качества
EDIT_MIN_LENGTH_RATIO=0.2            # минимум 20% от исходной длины
EDIT_MAX_LENGTH_RATIO=1.3            # максимум 130% от исходной длины

# Отчёты и режимы
EDIT_REPORT=true                     # создавать *_edit_report.json
EDIT_DRY_RUN=false                   # тестовый прогон без записи файлов

# TRANSCRIBE_OUT defaults to ./transcribe if not set
