# Faster-Whisper v2 - Автоматическая обработка файлов

## Описание

Версия 2 скрипта транскрибации с автоматической обработкой множественных файлов. Скрипт может извлекать аудио из видеофайлов и транскрибировать как видео, так и аудиофайлы.

## Структура папок

```
FasterWhisperLocal/
├── audio_in/          # Входные аудиофайлы
├── audio_out/         # Готовые WAV 16 kHz mono
├── transcribe/        # Текстовые транскрипты
├── video_in/          # Входные видеофайлы
├── tests/             # Pytest-модуль для регрессий
├── transcribe_v2.py   # Основной скрипт пайплайна
├── README.md          # Главное описание (для GitHub)
├── README_v2.md       # Расширенное руководство (этот файл)
└── QUICK_START.md     # Краткая инструкция по запуску
```

## Поддерживаемые форматы

### Видеофайлы (папка `video_in/`)
- `.mp4`, `.avi`, `.mov`, `.mkv`, `.wmv`, `.flv`, `.webm`

### Аудиофайлы (папка `audio_in/`)
- `.wav`, `.mp3`, `.m4a`, `.flac`, `.ogg`, `.aac`

## Как использовать

### 1. Подготовка файлов
- Поместите видеофайлы в папку `video_in/`
- Поместите аудиофайлы в папку `audio_in/`
- Можно использовать обе папки одновременно

### 2. Запуск скрипта
```bash
python transcribe_v2.py
```

### 3. Результаты
- **Аудиофайлы** сохраняются в папке `audio_out/` в формате WAV
- **Транскрибация** сохраняется в папке `transcribe/` в формате TXT
- Имена файлов формируются как `<stem>_<tag>_<suffix>.wav`, где `tag` — `video`/`audio`, а `suffix` — исходное расширение файла (например, `mp4`, `mp3`). Это исключает перезаписи при одинаковом stem у разных форматов.

## Пример работы

```
🚀 Запуск Faster-Whisper v2 - Автоматическая обработка файлов
============================================================

✓ Аудио извлечено: video1.mp4 -> video1_video_mp4.wav
✓ Аудио скопировано: audio1.mp3 -> audio1_audio_mp3.wav

📁 Найдено файлов для транскрибации: 2

🤖 Загрузка модели Whisper...
✓ Модель 'large-v3' загружена на GPU

🎯 Начинаю транскрибацию 2 файлов...
------------------------------------------------------------

[1/2] Обработка: video1_video_mp4.wav
🎵 Транскрибация: video1_video_mp4.wav
✓ Транскрибация завершена за 45.23 сек
  Язык: ru (вероятность: 0.98)
  Длительность речи: 120.50 сек
  📄 Результат сохранен: video1.txt

[2/2] Обработка: audio1_audio_mp3.wav
🎵 Транскрибация: audio1_audio_mp3.wav
✓ Транскрибация завершена за 23.15 сек
  Язык: ru (вероятность: 0.95)
  Длительность речи: 67.30 сек
  📄 Результат сохранен: audio1.txt

============================================================
✅ Обработка завершена!
   Успешно обработано: 2/2 файлов
   Результаты сохранены в папке 'transcribe'
   Аудиофайлы сохранены в папке 'audio_out'
```

## Требования

### Системные зависимости
- **FFmpeg** - для извлечения аудио из видео
  - Windows: скачайте с https://ffmpeg.org/download.html
  - Linux: `sudo apt install ffmpeg`
  - macOS: `brew install ffmpeg`

### Python зависимости
1. Создайте окружение Python 3.11–3.12 (рекомендуем `python -m venv .venv`).
2. Активируйте его и установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. (Опционально) Для ускорения на Windows с CUDA выполните:
   ```bash
   pip install --upgrade --index-url https://download.pytorch.org/whl/cu121 torch torchvision torchaudio
   ```

## Особенности

### Автоматическое извлечение аудио (условная проверка FFmpeg)
- Из видеофайлов автоматически извлекается аудио в формате WAV
- Частота дискретизации: 16kHz, моно
- Оптимально для распознавания речи
- FFmpeg проверяется и требуется только если в папках найдены видео или несжатые аудио, требующие перекодирования. Для готовых WAV FFmpeg не обязателен.

### VAD-фильтр
- Автоматически удаляет паузы и тишину
- Улучшает качество транскрибации
- Настраиваемый порог тишины (500 мс)

### Адаптивная загрузка модели
- Сначала пытается загрузить на GPU (быстрее)
- При ошибке автоматически переключается на CPU
- Поддерживает разные типы вычислений

### Обработка ошибок
- Подробные сообщения об ошибках
- Продолжение работы при ошибках отдельных файлов
- Статистика успешных/неуспешных операций

### Кеш транскриптов
- Если для подготовленного WAV уже существует TXT-файл, который новее WAV, транскрибация пропускается. Это экономит время при повторных запусках.

## Настройки

### Переменные окружения
- Конфигурация путей и параметров модели читается из `.env`. Скопируйте `.env.example` в `.env` и при необходимости измените значения.
- Основные переменные:
  - `VIDEO_IN`, `AUDIO_IN`, `AUDIO_OUT`, `TRANSCRIBE_OUT` — каталоги для входных и выходных файлов
  - `WHISPER_MODEL` — размер используемой модели (по умолчанию `small`)
  - `WHISPER_DEVICE` — `cuda` или `cpu` (значение `auto` выбирает доступное устройство автоматически)
  - `WHISPER_BEAM_SIZE` — ширина поиска, `WHISPER_VAD_FILTER` — включение VAD, `WHISPER_VAD_MIN_SILENCE` — минимальная длина тишины в миллисекундах
  - `WHISPER_CPU_THREADS` — число потоков CPU (0 оставляет автонастройку `ctranslate2`)
  - `WHISPER_NUM_WORKERS` — количество воркеров декодера (1–4; больше = быстрее, но выше нагрузка)
  - `SKIP_FRESH_AUDIO` — пропуск повторной перекодировки, если WAV-файл не устарел
  - `WHISPER_COMPUTE_TYPE` — тип вычислений (`int8`, `int8_float16`, `float16` и т.д.). По умолчанию: `int8` на CPU, `float16` на CUDA.

### Модель
- По умолчанию используется модель `small`, подходящая для систем без GPU.
- Для смены модели укажите значение переменной `WHISPER_MODEL` (варианты: `tiny`, `base`, `small`, `medium`, `large-v2`, `large-v3`).

### Оптимизация производительности
- `WHISPER_CPU_THREADS` — ограничивает количество потоков CPU, используемых CTranslate2 (0 = авто).
- `WHISPER_NUM_WORKERS` — число потоков разбора сегментов; 2–4 ускоряют запись результатов на длинных файлах.
- При использовании CUDA моделирование выполняется в `float16`, что ускоряет работу и экономит VRAM. При падении загрузки GPU скрипт автоматически переключится на CPU (`int8`).

## Устранение неполадок

### FFmpeg не найден
```
✗ Ошибка извлечения аудио: [Errno 2] No such file or directory: 'ffmpeg'
```
**Решение**: Установите FFmpeg и добавьте в PATH

### Ошибка GPU
```
⚠️ Ошибка загрузки на GPU: CUDA out of memory
```
**Решение**: Скрипт автоматически переключится на CPU

### Пустые результаты
```
❌ Не найдено файлов для обработки!
```
**Решение**: Проверьте, что файлы находятся в правильных папках и имеют поддерживаемые форматы
